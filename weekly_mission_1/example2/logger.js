//logger.js

// Esta es una función que se guarda en este módulo como info
exports.info= (message) => {
    console.log(`info: ${message}`)
}

// Esta es una función que se fuardara en este modulo como verbose.

exports.verbose= (message) => {
    console.log(`verbose:${message}`)
}

// Mi función de prueba.
exports.chanchito  = (m) => {
    console.log(`Chanchito esta: ${m}`)
}